import 'package:flutter/material.dart';

class RegistrationFlow extends StatelessWidget {
  const RegistrationFlow({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Registration Flow'));
  }
}
